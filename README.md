# LLM-RAG-Assist
LLM-RAG-Assist provides tools to trim both PDF and EPUB within page range(s), e.g.:, the 'introduction', 'about the author', 'index', etc. A complete waste of time/resources. Also. includes a utility to convert the EPUB to PDF if your LLM model/system you're using only supports PDFs. 
